import type { Locale } from '@/paraglide/runtime';

/** Language tags used in document metadata and crawlable language links. */
export const HTML_LANG: Record<Locale, string> = {
  en: 'en',
  zh: 'zh-CN',
  ja: 'ja',
};

export const OG_LOCALE: Record<Locale, string> = {
  en: 'en_US',
  zh: 'zh_CN',
  ja: 'ja_JP',
};
